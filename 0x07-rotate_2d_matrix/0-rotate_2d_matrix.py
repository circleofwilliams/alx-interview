#!/usr/bin/python3
"""rotate_2d_matrix module
"""


def rotate_2d_matrix(matrix):
    """A function to rotate a 2D matrix
    """
    matrix_len = len(matrix)
    for i in range(k):
        matrix_index = k - 1
        for j in range(k):
            matrix[i][j] = dup[matrix_index][i]
            matrix_index -= 1
